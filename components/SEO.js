"use client"

export default function SEO({ title, description, keywords, url, image }) {
  return (
    <>
      {/* This component is now client-side only and will be used alongside the server-side metadata */}
      {/* Additional client-side only meta tags can be added here if needed */}
    </>
  );
} 